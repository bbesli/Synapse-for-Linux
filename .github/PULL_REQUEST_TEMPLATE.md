**What does this change? / Bu değişiklik ne yapıyor?**

**Tested on / Test edilen donanım**
- Device and connection:
- Distribution:

**Checklist**
- [ ] `cargo fmt --all`, `cargo clippy --workspace --all-targets -- -D warnings` and `cargo test --workspace` pass
- [ ] Device settings changed during testing were restored
- [ ] `docs/PROTOCOL.md` and the READMEs are updated if the protocol or device support changed
- [ ] No Razer code, logos or other files are included
